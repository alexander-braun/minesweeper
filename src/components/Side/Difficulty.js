import React from 'react'


function Difficulty(props) {
    return (
        <React.Fragment>
            <div>
                Difficulty
            </div>
        </React.Fragment>
    )
}

export default React.memo(Difficulty)
